import type { RefKeys } from './types';

export const REF_KEYS: RefKeys = {
  blog: 'slug',
  nav: 'id',
  page: 'slug',
  podcast: 'slug',
  post: 'slug',
  schoolSubject: 'slug',
  teacher: 'slug',
  workinggroup: 'slug',
};
