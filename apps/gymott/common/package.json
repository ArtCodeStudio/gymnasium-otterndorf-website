{
  "name": "@gymott/common",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "scripts": {
    "build": "yarn run typegen && tsc",
    "typegen": "yarn workspace @gymott/tools run typegen",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}
